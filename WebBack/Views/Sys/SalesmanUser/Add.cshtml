@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_LayoutHomeDialog.cshtml";
}
@section scripts{
    <script type="text/javascript">
        <!--
        var zTree;
        $(this).ready(function () {
            $.formValidator.initConfig({ formID: "form1", onSuccess: function () { return add(); } });
            $("#Model_SysSalesmanUser_UserName").formValidator({ onShow: "&nbsp;", onFocus: "由3到20个数字、英文字母或下划线组成", onCorrect: "&nbsp;" }).regexValidator({ min: 3, max: 20, regExp: "^[0-9a-zA-Z_]{3,20}$", onError: "由3到20个数字、英文字母或下划线组成" });
            $("#Model_SysSalesmanUser_FullName").formValidator({ onShow: "&nbsp;", onFocus: "不超过50个字符", onCorrect: "&nbsp;" }).inputValidator({ min: 1, max: 50, empty: { leftEmpty: false, emptyError: "不能有空格" }, onError: "不超过50个字符" });
            $("#Model_SysSalesmanUser_Email").formValidator({ onShow: "&nbsp;", onFocus: "邮箱格式且不超过100个字符", onCorrect: "&nbsp;", empty: true, onEmpty: "&nbsp;" }).inputValidator({ min: 0, max: 100, onError: "邮箱格式且不超过100个字符" }).regexValidator({ regExp: regexEnum.email, onError: "邮箱格式且不超过100个字符" });
            $("#Model_SysSalesmanUser_PhoneNumber").formValidator({ onShow: "&nbsp;", onFocus: "手机号码格式", onCorrect: "&nbsp;", empty: true, onEmpty: "&nbsp;" }).inputValidator({ min: 0, max: 11, onError: "手机号码格式且不超过11个字符" }).regexValidator({ regExp: regexEnum.mobile, onError: "手机号码格式且不超过11个字符" });

        });

        function add() {
            art.dialog.confirm(
'确定要新建？',
function () {
    var postData = $("#form1").serializeArray();

    $.lumos.postJson({
        url: "../SalesmanUser/Add",
        data: postData,
        success: function (d) {

            if (d.result == resultType.success) {
                $.lumos.parentDialog().listTable.loadData();
                $.lumos.tips(d.message,true);
            }
            else {
                $.lumos.tips(d.message);
            }

        }
    });
});
            return false;
        }

        //-->
    </script>
}


<div class="gbr-row">
    <form id="form1" name="form1" method="post">
        @Html.AntiForgeryToken()
        <table class="f-tb" cellpadding="0" cellspacing="1">
            <tr>
                <td class="f-tb-t" style="width:50px;">
                    用户名：
                </td>
                <td class="f-tb-c">
                    YW
                    <input name="Model.SysSalesmanUser.UserName" class="input-control" autocomplete="off" type="text" id="Model_SysSalesmanUser_UserName" style="width: 128px;" clear="on" />
                    <span id="Model_SysSalesmanUser_UserNameTip"></span>
                </td>
            </tr>
            <tr>
                <td class="f-tb-t">
                    姓名：
                </td>
                <td class="f-tb-c">
                    <input name="Model.SysSalesmanUser.FullName" class="input-control" type="text" id="Model_SysSalesmanUser_FullName" style="width: 150px;" />
                    <span id="Model_SysSalesmanUser_FullNameTip"></span>
                </td>
            </tr>
            <tr>
                <td class="f-tb-t">
                    E-mail：
                </td>
                <td class="f-tb-c">
                    <input name="Model.SysSalesmanUser.Email" class="input-control" type="text" id="Model_SysSalesmanUser_Email" style="width: 150px;" />
                    <span id="Model_SysSalesmanUser_EmailTip"></span>
                </td>
            </tr>
            <tr>
                <td class="f-tb-t">
                    手机：
                </td>
                <td class="f-tb-c">
                    <input name="Model.SysSalesmanUser.PhoneNumber" class="input-control" type="text" id="Model_SysSalesmanUser_PhoneNumber" style="width: 150px;" />
                    <span id="Model_SysSalesmanUser_PhoneNumberTip"></span>
                </td>
            </tr>
            <tr style="display:none">
                <td class="f-tb-t"></td>
                <td class="f-tb-c">
                    <input type="submit" name="btn_Submit" id="btn_Submit" value="确定" class="btn btn-sure" />
                    <input type="button" name="btn_Cancle" id="btn_Cancle" value="关闭" class="btn btn-close dialog-close" />
                </td>
            </tr>
        </table>
    </form>
</div>

